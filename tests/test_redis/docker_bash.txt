1.common commands
update <PUBLIC_IP> to your public ip

docker compose up -d
docker compose down

2. sentinel mode
# docker compose -f docker-compose.yml up -d redis-master redis-slave1 redis-slave2
# docker compose -f docker-compose.yml up -d sentinel1 sentinel2 sentinel3

redis-cli -h <ip> -p <port> sentinel get-master-addr-by-name mymaster

3. cluster mode
redis-cli -c -h <ip> -p <port>


Real production should involve all instances being exposed to a public IP address within a local area network