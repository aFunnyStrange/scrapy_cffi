# ProcessManager example

Start the server:

```bash
python examples/process_manager/server.py
```

Then run the client in another terminal:

```bash
python examples/process_manager/client.py
```

The example registers a function, a class, and an object instance, then calls
them through `ProcessManager`.
